# 国际化

## 实现

界面消息使用 ICU MessageFormat。语言源目录位于 `res/i18n/<BCP47>.json`，`locales.json` 声明基础语言、可用语言和别名。每个语言目录都是独立完整的消息目录；构建时 `check_i18n` 只比较各目录的 message key 集合，并复制通过比较的目录到运行时的 `i18n/`。

消息 ID 和调用参数类型定义在 `include/i18n_messages.hpp`。调用形式为 `tr(Msg::SetupGame)`、`tr(msg::TemplateWelcome{username})`、`tr(msg::SlotNumGuess{count})`。普通语言直接使用 ICU 的 `plural`、`selectordinal`、`select`、`number` 等 MessageFormat 能力及 ICU 自带的复数、数字、日期规则。

```json
{
  "locale": "en-US",
  "status": "maintained",
  "messages": {
    "slot.num_guess": "{count, plural, one {You made # guess.} other {You made # guesses.}}"
  }
}
```

运行时按请求标签选择目录：先尝试别名和逐段缩短的标签，找不到时使用基础语言。选中的目录直接提供全部消息，不再做消息级回退或父语言链。模板的解析、缺参和格式错误交给 ICU；程序只负责把 C++ 参数传给 ICU，以及对最终文本做终端安全过滤。

## Locale

```json
"locale": {"language": "la", "calendar": "gregorian", "timeZone": "UTC"}
```

语言、时区和日历独立配置。所有资源和内部文本固定为 UTF-8。`timeZone` 接受 ICU 时区 ID 或 `local`，`calendar` 默认为 `default`。

## 构建

构建不会生成头文件或执行翻译语义检查。`check_i18n` 只检查每个语言文件是否与基础语言拥有相同的 message key 集合，并将通过的目录复制到构建输出；缺少或多出 key 的目录会被跳过。运行时加载资源时由 ICU 处理 MessageFormat 错误。
